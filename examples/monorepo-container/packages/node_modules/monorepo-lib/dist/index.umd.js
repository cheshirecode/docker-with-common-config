(function (global, factory) {
	typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports) :
	typeof define === 'function' && define.amd ? define(['exports'], factory) :
	(factory((global.index = {})));
}(this, (function (exports) { 'use strict';

var createGUID = function createGUID() {
  return "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g, function (c) {
    var r = Math.random() * 16 | 0;
    var v = c === "x" ? r : r & 0x3 | 0x8;
    return v.toString(16);
  });
};

var formatStringToNumber = function formatStringToNumber(n, dec) {
  var result = 0;
  if (!isNaN(n)) {
    result = parseFloat(n.toString().replace(/,/g, ""));
    if (dec) {
      result = result.toFixed(dec);
    }
    result = Number.isNaN(result) ? 0 : +result;
  }
  return result;
};



var Utils = Object.freeze({
	createGUID: createGUID,
	formatStringToNumber: formatStringToNumber
});

var SampleModule = {
  Utils: Utils
};

var index = (function (name) {
  var subModule = SampleModule[name];
  if (typeof subModule === "undefined") {
    console.trace(); //eslint-disable-line no-console
    throw Error("Invalid reference to sub-subModule ", name);
  }
  return subModule;
});

exports.Utils = Utils;
exports.default = index;

Object.defineProperty(exports, '__esModule', { value: true });

})));
